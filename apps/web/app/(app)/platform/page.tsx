import type { ReactElement } from "react";

import { TenantsView } from "../../../features/platform/components/tenants-view";

export default function Page(): ReactElement {
  return <TenantsView />;
}
