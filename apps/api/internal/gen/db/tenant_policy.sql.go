// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.1
// source: tenant_policy.sql

package db

import (
	"context"

	"github.com/google/uuid"
	"github.com/jackc/pgx/v5/pgtype"
)

const academicGetCalendarPolicy = `-- name: AcademicGetCalendarPolicy :one
select config from tenant_policies
where tenant_id = $1 and kind = 'calendar' and effective_from <= $2
order by effective_from desc, version desc
limit 1
`

type AcademicGetCalendarPolicyParams struct {
	TenantID      uuid.UUID   `json:"tenant_id"`
	EffectiveFrom pgtype.Date `json:"effective_from"`
}

// Reads the tenant's current "calendar" policy (owned by the platform/tenant
// domain, table tenant_policies): the effective row as of asOf, i.e. the
// highest version whose effective_from has passed. Read-only, additive --
// this module does not write tenant_policies.
func (q *Queries) AcademicGetCalendarPolicy(ctx context.Context, arg AcademicGetCalendarPolicyParams) ([]byte, error) {
	row := q.db.QueryRow(ctx, academicGetCalendarPolicy, arg.TenantID, arg.EffectiveFrom)
	var config []byte
	err := row.Scan(&config)
	return config, err
}
