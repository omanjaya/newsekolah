// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.1
// source: tenants.sql

package db

import (
	"context"

	"github.com/google/uuid"
	"github.com/jackc/pgx/v5/pgtype"
)

const createTenant = `-- name: CreateTenant :one
insert into tenants (slug, name, education_level, timezone, locale, status, plan, primary_domain)
values ($1, $2, $3, $4, $5, $6, $7, $8)
returning id, slug, name, education_level, timezone, locale, status, plan, primary_domain, created_at, updated_at
`

type CreateTenantParams struct {
	Slug           string      `json:"slug"`
	Name           string      `json:"name"`
	EducationLevel string      `json:"education_level"`
	Timezone       string      `json:"timezone"`
	Locale         string      `json:"locale"`
	Status         string      `json:"status"`
	Plan           string      `json:"plan"`
	PrimaryDomain  pgtype.Text `json:"primary_domain"`
}

func (q *Queries) CreateTenant(ctx context.Context, arg CreateTenantParams) (Tenant, error) {
	row := q.db.QueryRow(ctx, createTenant,
		arg.Slug,
		arg.Name,
		arg.EducationLevel,
		arg.Timezone,
		arg.Locale,
		arg.Status,
		arg.Plan,
		arg.PrimaryDomain,
	)
	var i Tenant
	err := row.Scan(
		&i.ID,
		&i.Slug,
		&i.Name,
		&i.EducationLevel,
		&i.Timezone,
		&i.Locale,
		&i.Status,
		&i.Plan,
		&i.PrimaryDomain,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}

const getSingleTenant = `-- name: GetSingleTenant :one
select id, slug, name, education_level, timezone, locale, status, plan, primary_domain, created_at, updated_at from tenants order by created_at asc limit 1
`

func (q *Queries) GetSingleTenant(ctx context.Context) (Tenant, error) {
	row := q.db.QueryRow(ctx, getSingleTenant)
	var i Tenant
	err := row.Scan(
		&i.ID,
		&i.Slug,
		&i.Name,
		&i.EducationLevel,
		&i.Timezone,
		&i.Locale,
		&i.Status,
		&i.Plan,
		&i.PrimaryDomain,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}

const getTenantByDomain = `-- name: GetTenantByDomain :one
select t.id, t.slug, t.name, t.education_level, t.timezone, t.locale, t.status, t.plan, t.primary_domain, t.created_at, t.updated_at from tenants t
left join tenant_domains d on d.tenant_id = t.id
where t.primary_domain = $1 or d.domain = $1
limit 1
`

func (q *Queries) GetTenantByDomain(ctx context.Context, primaryDomain pgtype.Text) (Tenant, error) {
	row := q.db.QueryRow(ctx, getTenantByDomain, primaryDomain)
	var i Tenant
	err := row.Scan(
		&i.ID,
		&i.Slug,
		&i.Name,
		&i.EducationLevel,
		&i.Timezone,
		&i.Locale,
		&i.Status,
		&i.Plan,
		&i.PrimaryDomain,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}

const getTenantByID = `-- name: GetTenantByID :one
select id, slug, name, education_level, timezone, locale, status, plan, primary_domain, created_at, updated_at from tenants where id = $1
`

func (q *Queries) GetTenantByID(ctx context.Context, id uuid.UUID) (Tenant, error) {
	row := q.db.QueryRow(ctx, getTenantByID, id)
	var i Tenant
	err := row.Scan(
		&i.ID,
		&i.Slug,
		&i.Name,
		&i.EducationLevel,
		&i.Timezone,
		&i.Locale,
		&i.Status,
		&i.Plan,
		&i.PrimaryDomain,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}

const getTenantBySlug = `-- name: GetTenantBySlug :one
select id, slug, name, education_level, timezone, locale, status, plan, primary_domain, created_at, updated_at from tenants where slug = $1
`

func (q *Queries) GetTenantBySlug(ctx context.Context, slug string) (Tenant, error) {
	row := q.db.QueryRow(ctx, getTenantBySlug, slug)
	var i Tenant
	err := row.Scan(
		&i.ID,
		&i.Slug,
		&i.Name,
		&i.EducationLevel,
		&i.Timezone,
		&i.Locale,
		&i.Status,
		&i.Plan,
		&i.PrimaryDomain,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}

const searchTenants = `-- name: SearchTenants :many
select id, slug, name, education_level, timezone, locale, status, plan, primary_domain, created_at, updated_at from tenants where name ilike $1 or slug ilike $1 order by name limit 20
`

func (q *Queries) SearchTenants(ctx context.Context, name string) ([]Tenant, error) {
	rows, err := q.db.Query(ctx, searchTenants, name)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []Tenant{}
	for rows.Next() {
		var i Tenant
		if err := rows.Scan(
			&i.ID,
			&i.Slug,
			&i.Name,
			&i.EducationLevel,
			&i.Timezone,
			&i.Locale,
			&i.Status,
			&i.Plan,
			&i.PrimaryDomain,
			&i.CreatedAt,
			&i.UpdatedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}
