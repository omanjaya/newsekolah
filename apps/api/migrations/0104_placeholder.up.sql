-- Placeholder: this number was skipped when the migration history was
-- written. It keeps the sequence gap-free for infra/scripts/check-migrations.sh
-- and does nothing. Databases already past this version never run it.
select 1;
