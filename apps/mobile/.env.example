# Base URL of the newsekolah API. In development this points at the local API server.
EXPO_PUBLIC_API_URL=http://localhost:8080

# Tenant slug used when the app cannot resolve one yet (single-tenant deployments,
# or before the user has picked a school on the school-picker screen).
EXPO_PUBLIC_DEFAULT_TENANT=
