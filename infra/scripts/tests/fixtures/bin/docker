#!/usr/bin/env bash
# Not exercised by the harness (the fixture monitor.env disables the
# containers and errors checks in every test); fails loudly instead of
# reaching the real Docker daemon if a test ever invokes it unexpectedly.
echo "fake docker: unexpected invocation: $*" >&2
exit 1
