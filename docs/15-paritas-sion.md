# 15. Paritas dengan SION

Catatan kerja untuk menyamakan logika bisnis newsekolah dengan SION, aplikasi asal yang kodenya ada di `reference/sion-rebuild-go`. Dokumen ini menjawab satu pertanyaan: sampai mana pekerjaan ini, dan apa yang tersisa. Perbarui setiap kali ada bagian yang selesai.

Posisi terakhir: 25 September 2026 (dua item ledger perpustakaan: barcode kartu anggota, cetak kartu massal). **Barcode gambar pada kartu anggota**: `library/service/print.go`'s `PrintMemberCard` sebelumnya lewat `documents.Renderer` (perender HTML->PDF berbasis teks polos, membuang tag `<img>` -- lihat komentar paket) sehingga nomor anggota cuma tercetak sebagai teks; sekarang merender langsung dengan `fpdf`, memakai layout kartu ATM 85,6x54mm (`domain/cards.go`'s `CardPosition`, 2 kolom x 5 baris per A4, dari aplikasi lama `library_cards.go`) dan Code 128 gambar nomor anggota lewat `generateCode128PNG` yang sama dipakai label eksemplar (`service/labels.go`) -- satu jalur kode barcode untuk keduanya. Surat keterangan bebas pustaka tidak disentuh (tetap lewat `documents.Renderer`, tidak butuh barcode). **Cetak kartu anggota massal**: endpoint baru `POST /v1/library/members/cards` (`printLibraryMemberCards`, permission `manage_library_members`, `openapi/modules/library.yaml`) mencetak sampai 500 kartu (`domain.CardMaxPerPrint`) dalam satu PDF, dipilih lewat tepat satu dari tiga mode -- daftar `member_ids` eksplisit (urutan dipertahankan, query `GetMembersByIDs` baru), `member_type_id`, dan/atau `class_id` (keanggotaan aktif lewat `enrollments`, query `ListMembersForCardPrint` baru, keduanya bisa digabung). Layar Anggota (`members-view.tsx`) mendapat kolom pilihan baris (`selectionColumn`, pola sama `copies-browser-view.tsx`) plus bilah cetak `MemberCardPrintBar` untuk baris terpilih, dan tombol "cetak sesuai filter jenis" saat filter jenis anggota aktif. Diuji integrasi Postgres asli (`member_cards_integration_test.go`): tiga mode seleksi, gabungan tipe+kelas, penolakan kombinasi tidak sah (tanpa seleksi, id dicampur filter, id melebihi 500), filter tanpa hasil (404), dan paginasi lewat 10 kartu per halaman; test kedua membuktikan barcode sungguhan tertanam (marker `/Subtype /Image` pada PDF, bukan lagi kartu berbasis teks murni). Tidak ada migrasi (query baru memakai tabel `library_members`/`enrollments` yang sudah ada). `go test ./...` penuh (real Postgres via testcontainers), `pnpm typecheck && pnpm lint && pnpm test`, dan `check-migrations.sh` lulus.

Sesi paralel 25 September 2026 (sesi backend: laporan PDF izin keluar, heartbeat presence, publish realtime dari worker). Menutup dua baris "Pekerjaan yang ditunda" di bawah plus satu item tambahan yang ditemukan selama kerja. **Laporan PDF izin keluar**: ternyata sudah selesai dari migrasi pusat laporan generik sebelumnya (`reports/service.RunDocument` + `reportdoc`) -- baris tabel di bawah cuma belum diperbarui. Ditambah bukti ujung-ke-ujung baru: `TestExportReportExitPermitYearlyPDFHasLetterhead` (`apps/api/internal/wiring/reports_integration_test.go`, Postgres asli lewat `dbtest`) memanggil `RunDocument` yang sama dipakai endpoint `GET /v1/reports/{reportKind}/export`, dengan kop laporan tenant sungguhan tersimpan lewat `school.Service.UpdateReportHeader`, memverifikasi kop muncul di sel A1 XLSX dan keluaran PDF valid. **Heartbeat presence**: `apps/api/cmd/api/ws.go`'s `wsMeHandler` kini menjalankan `heartbeatPresence` (goroutine baru, tiap 30 detik lewat `presenceHeartbeatInterval`) selama koneksi `/ws/me` hidup, bukan cuma sekali saat connect; berhenti bersih lewat `client.Done()` saat socket ditutup. Kedaluwarsa sisi server (`realtime.Presence.Snapshot`'s filter ttl, plus `RedisPresenceStore`'s trim per replika) sudah ada sebelumnya -- ditambah `presence_test.go` dan `ws_test.go` (websocket sungguhan lewat `httptest`) yang mengujinya.

Sesi yang sama, item tambahan: **Publish realtime dari worker** (ditemukan saat menyelidiki cara kerja presence/hub, bukan bagian dari dua baris tabel "Pekerjaan yang ditunda"): `cmd/worker/main.go` (proses `WORKER_INLINE=false`) tidak pernah membangun `realtime.Hub` sama sekali, jadi notifikasi yang dibuat job latar belakang (pengingat jatuh tempo perpustakaan, laporan terjadwal siap, dan job lain manapun yang memanggil `notifications` service's realtime publish) tersimpan di inbox tapi tidak pernah didorong ke klien `/ws/me` yang tersambung sampai halaman dimuat ulang. Diperbaiki dengan hub publish-only di worker (`realtime.NewHub` memakai broadcaster Redis yang sama `cmd/api` pakai, tanpa `mountRealtimeRoutes`/`Upgrade` -- worker tidak pernah melayani websocket, jadi `hub.Subscribe` tak pernah dipanggil di sana, degradasi ke no-op tanpa Redis), disuntik ke `notifications.Dependencies.Realtime`, `permits.Dependencies.Hub`, dan `attendance.Dependencies.Hub`. Sambil di situ, ditemukan dan diperbaiki bug lebih dalam: `notifications/service.RealtimePublisher.Publish` sebelumnya membaca tenant dari `httpx.TenantIDFromContext(ctx)`, yang HANYA terisi untuk request HTTP -- job River (baik di worker terpisah maupun mode inline `cmd/api`) tidak pernah membawa nilai itu di context-nya, jadi push realtime dari alur job (pengingat jatuh tempo, dll) senyap gagal bahkan sebelum perbaikan hub ini, di KEDUA mode deployment. Diperbaiki dengan mengubah signature `RealtimePublisher.Publish` supaya menerima `tenantID` eksplisit dari pemanggil (`notify.go`'s `notifyOne` sudah punya `tenantID` di parameternya) alih-alih dari context. Diuji: `TestPublishOnlyHubReachesSubscriberOnSeparateHub` (`internal/platform/realtime/hub_test.go`, broadcaster palsu di proses -- repo belum punya dependensi testcontainers-Redis atau miniredis) membuktikan publish dari hub tanpa subscriber lokal (meniru worker) sampai ke klien websocket sungguhan di hub terpisah (meniru `cmd/api`) lewat broadcaster bersama. Semua perubahan diverifikasi: `go build`/`go vet`/`go test ./...` (Postgres asli lewat testcontainers) dan `pnpm typecheck && pnpm lint && pnpm test` lulus; tidak ada migrasi baru.

Sesi paralel 25 September 2026 (sesi audit operasi OpenAPI tak terjangkau web). Baris "46 operasi belum terjangkau" di ringkasan di bawah berasal dari hitungan grep dangkal (`.GET("path"`/`.POST("path"` literal saja) yang tidak menghitung endpoint ekspor biner yang dipanggil lewat `fetch` mentah dengan path dinamis (`` `${API_URL}/v1/.../${id}/export` ``) -- pola yang dipakai hampir semua unduhan XLSX/PDF di `apps/web` karena `openapi-fetch` selalu mem-parse respons sebagai JSON. Menghitung ulang dengan mencocokkan operationId x path x method satu per satu terhadap kode web (termasuk pola `fetch` dinamis itu, dan membedakan method yang berbagi path yang sama, mis. `POST`/`PUT`/`GET` semuanya di `/v1/grading/grade-ranges`) menemukan hanya 20 operasi API yang betul-betul tidak dipanggil kode web, bukan 46. Sembilan di antaranya di luar cakupan sesi ini dan dijelaskan satu per satu di bawah (websocket/presence, webhook WhatsApp, kesehatan server dan refresh token internal, definisi peran, kartu/cetak anggota perpustakaan). Sebelas sisanya genuinely unreachable dari web; sembilan dari sebelas itu ternyata sudah punya jalan pintas fungsional yang setara (endpoint massal/bulk yang sudah dipakai UI mencakup kasus satu-item, atau field yang datanya sudah identik dengan yang dikembalikan endpoint daftar) sehingga nilainya bagi sekolah mendekati nol -- dijelaskan satu per satu, tidak dibuatkan layar semu hanya demi kelengkapan. Dua sisanya nilainya nyata dan dikerjakan sesi ini: **Nonaktifkan penugasan mengajar** (`PUT /v1/academic/teaching-assignments/{assignmentId}`, `updateTeachingAssignment`) -- tab "Guru pengampu" di detail kelas (`apps/web/features/school/components/teaching-panel.tsx`) sebelumnya hanya punya hapus permanen (`Trash2`) untuk penugasan mengajar; kini ada aksi nonaktifkan (`Ban`) di samping hapus, plus bagian "Tampilkan penugasan nonaktif" yang bisa diaktifkan kembali (`RotateCcw`) -- riwayat penugasan (dipakai jurnal, presensi, nilai) tidak lagi hilang hanya karena guru cuti atau kelasnya direshuffle. **Catatan jadwal saat diubah** (`GET /v1/schedules/{scheduleId}`, `getSchedule`) -- form ubah blok jadwal (`schedule-form.tsx`) sebelumnya hanya punya `ScheduleBlock` (bentuk baris gabungan yang dipakai kisi jadwal), yang tidak membawa field `notes`/`mutation_policy`; menyimpan perubahan jadwal diam-diam menghapus catatan yang sudah ada karena form tidak pernah tahu catatan itu ada. Form kini memuat detail penuh sekali saat dialog ubah dibuka lewat `useScheduleQuery` baru, menampilkan field catatan terisi, dan menonaktifkan tombol Simpan (dengan pesan "Coba lagi") selama pemuatan gagal supaya catatan lama tidak pernah tertimpa tanpa sengaja. Kedua fitur diuji lewat interaksi (klik nonaktifkan/aktifkan lagi, dan pemuatan/error/pengisian catatan) mengikuti pola test komponen yang sudah ada; tidak ada perubahan API atau migrasi. Rincian daftar lengkap 20 operasi, alasan tiap pengecualian, dan peringkat nilai kesembilan operasi yang tidak dikerjakan ada di [Lampiran F](analysis/openapi-web-gaps-2026-09-25.md).

Sesi paralel 25 September 2026 (sesi izin lihat-nilai untuk Kepala Sekolah). Modul penilaian tadinya cuma punya satu izin `manage_grades` yang menggerbangi baca maupun tulis, jadi peran `principal` (Kepala Sekolah, migrasi 0117) tidak bisa melihat nilai sama sekali (celah yang sudah dicatat eksplisit di komentar `role_defaults.go`). Menutup celah itu: permission baru `view_grades` (`internal/platform/authz/permissions.go`, grup "grading") ditambahkan ke setiap peran yang sudah punya `manage_grades` (jadi `manage_grades` selalu menyiratkan `view_grades` -- tidak ada peran yang kehilangan akses baca) dan ke peran `principal`; migrasi `0118_grading_view_grades_permission` membackfill kedua hal itu untuk tenant yang sudah ada (idempoten -- baris `permissions` disisipkan dulu sebelum `role_permissions` mereferensikannya, karena `cmd/migrate`'s PostUp yang biasanya meng-upsert katalog izin baru jalan setelah migrasi SQL, bukan sebelum). Operasi baca-saja grading (`getGradebook`, `exportGradebook`, `listGradeRanges`, `listTPMappings`, `previewEraporExport`, `exportErapor`, `exportEraporLegacy`, plus laporan katalog `grading.report_scores` di modul reports) pindah `x-permission`-nya dari `manage_grades` ke `view_grades`; operasi tulis (simpan skor, komponen, publikasi, grade ranges, TP mapping) tetap `manage_grades`. Tidak ada mekanisme "salah satu dari" permission di level OpenAPI (`authz.Authorize` mengecek tepat satu `x-permission` per operasi) -- kepala sekolah membaca sekolah penuh lewat pola yang sudah ada di modul attendance: `GradingHandler.canViewAny` (baru, `transport/http/handler.go`) mem-bypass pemeriksaan `requireTeaches` di service layer untuk pemegang `view_reports` (izin pengawasan yang sudah dimiliki principal tapi TIDAK dimiliki guru biasa) atau `manage_master_data`, persis seperti `attendance.Actor.CanViewAll` memakai `view_reports` -- sehingga guru yang kebagian `view_grades` lewat implikasi `manage_grades` TIDAK ikut mendapat bypass sekolah-penuh (skop mereka tetap kelas sendiri lewat `requireTeaches`). Web: `lib/navigation.ts`'s entri "grading" pindah dari `permission: "manage_grades"` ke `anyPermission: ["manage_grades", "view_grades"]` (pola yang sama dipakai leave-requests/exit-permits); `grading-view.tsx` menambah `canViewGrades`/`canOpenAny` (kini `manage_master_data || view_reports`, mencerminkan `canViewAny` backend) sehingga kepala sekolah melihat pemilih kelas/mapel sekolah-penuh dan tab e-Rapor, tanpa tab TP-mapping/pengaturan; `GradebookSheet` sudah lama menerima prop `canManage` yang menyembunyikan semua kontrol edit/simpan/terbitkan, jadi cukup dialiri `canManageGrades` (tetap `false` untuk pemegang `view_grades` saja) tanpa perubahan di dalam `gradebook-sheet.tsx`/`gradebook-table.tsx`. Label izin `view_grades` ditambahkan ke `messages/id.json` dan `en.json`. Test: unit `role_defaults_test.go` (manage_grades menyiratkan view_grades, untuk principal dan untuk setiap peran secara umum), integrasi Postgres asli `platform/authz/grading_view_permission_integration_test.go` (principal lolos `authz.Authorize` untuk operasi baca lewat spec asli `api.GetSpec()` -- termasuk temuan bahwa oapi-codegen mem-PascalCase-kan `operationId` yang ikut ter-embed, jadi `authzStrictMiddleware` menerima "GetGradebook" bukan "getGradebook" -- dan mendapat 403 untuk operasi tulis; guru dan admin tidak berubah), integrasi Postgres asli `grading/view_grades_integration_test.go` (principal membaca gradebook dan ekspor kelas yang tidak diajarnya; guru lain tetap tidak bisa membaca kelas orang lain; guru pemilik kelas tetap bisa tanpa bypass; principal tetap ditolak service-level saat mencoba simpan skor/terbitkan), dan test komponen web baru `grading-view.test.tsx` (rendering baca-saja). Verifikasi: `go build`/`go vet`/`go test ./... -count=1` (Postgres asli via testcontainers) lulus semua, migrasi 0118 diverifikasi naik/turun/naik manual terhadap Postgres asli (lalu dihapus, bukan bagian suite permanen), `pnpm typecheck`/`pnpm lint`/`pnpm test` lulus semua 14 task turbo. `bash infra/scripts/check-migrations.sh` melaporkan celah 0118-0120 hilang (bukan regresi sesi ini -- nomor itu sengaja dilompati untuk dicadangkan bagi agen lain yang berjalan paralel di worktree lain; akan lulus begitu ketiganya digabung).

Posisi sebelumnya: 24 September 2026 (sesi UI/UX penilaian guru). Merombak lembar nilai (`apps/web/features/grading`, komponen `gradebook-*`) memakai pola presensi guru sebagai referensi kualitas: navigasi keyboard spreadsheet penuh di desktop (panah kanan/kiri kini juga pindah kolom, bukan cuma naik/turun/Enter; Tab sudah bekerja gratis lewat urutan DOM), validasi rentang skala nilai inline per sel (`sheet.scale.min/max` yang sudah dikirim API, border merah plus pesan di bar simpan, tombol simpan kolom yang bersangkutan ikut nonaktif) tanpa perubahan API, dan tempel kolom dari Excel (`lib/gradebook-paste.ts`, mengisi blok sel mulai dari sel yang difokus, lintas siswa maupun lintas komponen). Sel yang berubah ditandai border aksen (`changed` di `GradebookScoreCell`, pola sama dengan titik "berubah" di baris presensi). Rata-rata kini dihitung ulang langsung dari edit yang belum tersimpan (`lib/gradebook-scores.ts`'s `computeLiveAverage`, meniru `WeightedAverage` Go persis) alih-alih menunggu simpan; nilai rapor yang belum sinkron dengan edit tertunda mendapat titik kecil "diperbarui setelah disimpan". Kolom komponen menampilkan jumlah siswa yang belum diisi nilainya. Badge "Draf"/"Diterbitkan" ditambah di samping saklar terbitkan. Ponsel mendapat dua mode isi nilai yang bisa ditukar: "Per komponen" (baru, default -- satu komponen dipilih lewat chip yang menampilkan sisa kosong, lalu daftar siswa dengan input besar berturut-turut, cocok untuk mengoreksi satu kuis untuk seluruh kelas) dan "Per siswa" (kartu lama, satu siswa dengan semua komponennya). Draf lokal (`lib/gradebook-draft.ts`, pola sama `attendance-draft.ts`) menyimpan edit yang belum tersimpan ke localStorage per lembar kelas-mapel-semester dan menawarkan pemulihan setelah reload; bar simpan bawah (`packages/ui`'s `StickySaveBar`, komponen baru yang diekstrak dari `session-save-bar.tsx` presensi supaya kedua fitur memakai implementasi yang sama) menampilkan jumlah perubahan tertunda dan tombol "Simpan semua" yang mengirim setiap kolom yang berubah berurutan, dengan toast sukses berwaktu dan tombol "Coba lagi" saat gagal. `grade-ranges-editor.tsx` tidak disentuh selain memastikan gayanya tetap konsisten. Tidak ada perubahan API/migrasi -- semua data yang dibutuhkan (skala nilai per sel) sudah ada di respons `Gradebook` yang ada.

Sesi paralel 24 September 2026 (UI/UX alur harian guru: jurnal, kelas binaan, guru pengganti, presensi kerja). Empat layar dirombak memakai kelas kualitas alur presensi guru (session-card, chip status, bar simpan, draf localStorage) sebagai acuan. **Jurnal mengajar** (`apps/web/features/journal`): panel baru "Pelajaran terakhir" di atas tabel riwayat menampilkan jadwal mingguan guru diekspansi jadi baris per hari sekolah 7 hari terakhir (`lib/build-lesson-days.ts`, murni dan diuji, dikombinasikan dengan `useSchedulesQuery`+`useSchoolDaysQuery` yang sudah ada -- tanpa endpoint baru) dengan indikator terisi/belum dan satu ketukan membuka form isi; tiga field jurnal (topik/kegiatan/refleksi) dipindah ke `JournalFields` yang kini dipakai bersama oleh form berdiri sendiri dan tab Jurnal di sesi presensi (`SessionJournalPanel`), sehingga dua pintu masuk berperilaku identik; draf localStorage per kelas/mapel/tanggal (`lib/journal-draft.ts`) dengan prompt pulihkan. **Kelas binaan** (`apps/web/features/homeroom`): halaman tadinya cuma tabel `DataTable` padat kini dashboard sekilas pandang -- kartu stat (total/alpha/belum lengkap/izin menunggu), tiga kartu "Perlu perhatian hari ini" (siswa alpha/belum lengkap, tombol telepon dan WhatsApp ke wali lewat `lib/guardian-contact.ts`), "Izin menunggu persetujuan" (`useLeaveReviewQueueQuery` yang sudah ada, disaring ke `class_id` kelas binaan lewat `duties[].scope_id`), dan "Catatan pelanggaran" (dari `violation_count`/`violation_points` yang sudah ada di `AttendanceHomeroomEntry`) -- tabel lengkap tetap ada di bawahnya untuk pencarian/filter. **Guru pengganti** (`apps/web/features/substitutions`): kartu daftar kini menampilkan kelas, mata pelajaran, dan jam pelajaran yang digantikan, bukan cuma tanggal dan nama guru -- API `Substitution` dapat empat field opsional baru (`class_id`/`subject_id`/`start_period_id`/`end_period_id`) diisi lewat query `sqlc.embed` baru yang join ke `schedules` (`ListSubstitutionsIncomingWithSchedule`/`ListSubstitutionsOutgoingWithSchedule`, aman di-join karena `schedule_id` cascade on delete), diuji integrasi Postgres asli; badge "Hari ini" pada permintaan tanggal berjalan. **Presensi kerja** (`apps/web/features/staff-attendance`, `self-check-in-view.tsx`): jam besar hidup, dan "hari ini"/"minggu ini" kini dari server lewat endpoint self-service baru `GET /v1/staff-attendance/me/history` (`x-permission: authenticated`, pola sama seperti `scanStaffAttendance` -- resolve user dari context, bukan path param) alih-alih tebakan `localStorage` yang sebelumnya jadi satu-satunya sumber karena endpoint riwayat lama mensyaratkan permission `view_staff_attendance` yang tidak dimiliki pegawai biasa; diuji integrasi Postgres asli (karyawan lain tidak bisa melihat riwayat ini, dan kode status terkomputasi benar untuk hari tanpa jadwal). Alur diverifikasi ujung-ke-ujung di aplikasi berjalan dengan akun `guru` (data sementara ditambah lewat SQL untuk kasus yang seed tidak sediakan -- guardian_phone, permintaan pengganti berisi -- lalu dihapus setelah screenshot).

Sesi paralel 24 September 2026 (UI/UX alur harian kesiswaan: izin, presensi piket, pelanggaran, konseling). Lima layar (`apps/web/features/permits`, `apps/web/features/discipline`) dirombak memakai kelas kualitas alur presensi guru sebagai acuan, tanpa perubahan API. **Izin terencana**: antrean wali kelas dan antrean orang tua (yang sebelumnya hanya membuka dialog detail untuk apa pun) kini punya baris dengan aksi "Setujui" satu ketuk dan "Tolak" lewat popover alasan kecil (`leave-queue-row.tsx`, dipakai kedua antrean), plus aksi massal "Setujui terpilih" dengan keterangan eksplisit "tidak menambahkan catatan" -- setiap item di kedua endpoint antrean sudah dijamin server berada di tahap milik pemanggil sendiri, jadi tidak perlu membuka detail dulu untuk tahu bisa diputuskan. Form pengajuan kini menangkap foto bukti opsional (input kamera) di layar yang sama, dirantai sebagai panggilan kedua ke `useUploadEvidenceMutation` setelah `submit` berhasil (gagal unggah tidak membatalkan pengajuan, hanya memberi peringatan) -- sebelumnya bukti hanya bisa dilampirkan di kunjungan kedua ke halaman detail. **Izin keluar dan terlambat**: panel setujui/gerbang piket dan langkah pindai tahap siswa sendiri kini memakai `BarcodeScannerField` bersama (pemindai kamera/keyboard-wedge/input manual yang sama dipakai meja sirkulasi perpustakaan) menggantikan `ScanTokenInput` yang dihapus; panel gerbang menyimpan daftar "baru saja keluar" per sesi browser dengan waktu dan aksi sembunyikan (bukan pembatalan sungguhan di server -- diberi label jujur "Sembunyikan dari daftar ini"); kedua antrean review dapat filter cari nama/NIS. **Pelanggaran**: form catat kini bisa memilih beberapa siswa sekaligus (cari + daftar centang, pola sama dengan pemilih jenis pelanggaran), memanggil mutation satu-siswa yang sudah ada berulang per siswa terpilih dan menampilkan ringkasan poin/ambang SP per siswa setelah simpan. **Konseling**: form catatan mendapat draf localStorage dengan prompt pulihkan (pola sama `attendance-draft.ts`, file baru `lib/counseling-draft.ts`). `format-name.ts` pindah dari `attendance/lib` ke `lib/text` karena kini dipakai permits dan discipline juga. Belum dikerjakan: bukti foto untuk catatan pelanggaran (belum ada endpoint unggah untuk pelanggaran, tidak seperti bukti izin dan lampiran konseling -- perlu penambahan API sungguhan, di luar cakupan sesi ini), pratinjau ambang SP langsung saat memilih jenis pelanggaran (perilaku ringkasan-setelah-simpan yang sudah ada dipertahankan), dan pencatatan izin keluar/terlambat oleh piket atas nama siswa tanpa permintaan QR dari siswa lebih dulu (alur saat ini selalu diinisiasi siswa; mencatat atas nama siswa butuh endpoint baru sisi staf). Diverifikasi ujung-ke-ujung dengan API dan web sendiri (port 8103/3103) memakai akun admin/guru/gurubk/siswa/ortu terhadap Postgres dev bersama; data sementara (satu pengajuan izin, satu izin keluar) dihapus setelah tangkapan layar, dan satu catatan izin lama yang macet di status "approved" walau suratnya sudah terbit diperbaiki jadi "completed" (bug pra-eksisting yang tidak dikejar lebih jauh: pengecekan "satu izin aktif" tampaknya menganggap "approved" selalu masih berjalan, tanpa melihat apakah surat sudah terbit).

Sesi paralel 24 September 2026 (UI/UX alur harian keuangan dan tamu: loket pembayaran, papan tamu, komposer pengumuman). Tiga layar (`apps/web/features/billing`, `apps/web/features/visitors`, `apps/web/features/announcements`) dirapikan memakai kelas kualitas alur presensi guru sebagai acuan, tanpa perubahan API. **Loket pembayaran** (`payment-desk-view.tsx`): pemilih siswa berganti dari `Select` polos (mesti digulir manual di antara ratusan siswa) ke `Combobox` yang menyaring direktori yang sudah termuat di klien saat diketik -- direktori kecil untuk sekolah tunggal sehingga penyaringan klien memadai, `PersonPicker` async penuh (disebut `docs/05-shared-components.md`) belum dibangun dan di luar cakupan sesi ini. Status tagihan kini punya empat keadaan yang jelas dibedakan warna lewat token status yang sama dengan presensi (`lib/bill-status.ts`, `BillStatusBadge` baru dipakai bersama loket/daftar tagihan/detail tagihan): Lunas (hijau), Dibayar sebagian (oranye), Jatuh tempo (merah, belum lunas dan lewat tanggal jatuh tempo), dan Belum dibayar polos (belum jatuh tempo, tidak butuh perhatian). Memilih siswa kini menampilkan ringkasan `StatGrid` (total tunggakan, jumlah tagihan) sebelum daftar kartu tagihan, dan kartu tagihan terurut berdasar urgensi (jatuh tempo, lalu sebagian, lalu belum dibayar, baru lunas paling akhir) alih-alih urutan API apa adanya. Tombol kwitansi kini mencoba Web Share API dulu (`lib/receipt.ts`) sebelum jatuh ke buka tab baru, sehingga di HP kwitansi bisa langsung dibagikan ke WhatsApp tanpa mencari tab. Konfirmasi pembatalan pembayaran (`VoidPaymentDialog`, sudah ada sebelum sesi ini) sudah mensyaratkan alasan dan tombol destruktif terpisah -- tidak diubah. **Papan tamu** (`gate-board-view.tsx`): kotak cari (muncul begitu tamu di kampus lebih dari tiga) menyaring nama/instansi di klien, karena papan ini sudah realtime tanpa server-side search. **Komposer pengumuman** (`announcement-form.tsx`): form kini dua tab, "Tulis" dan "Pratinjau" -- tab pratinjau merender judul, ikon sematkan bila diaktifkan, kapan tampil (segera atau terjadwal), penerima (memakai `describeAudience` baru yang diekstrak dari `announcements-manage.tsx` supaya kolom "Dibaca" di tabel kelola dan pratinjau komposer tidak pernah mendeskripsikan audiens secara berbeda), dan isi lewat `SafeHtml` yang sama dipakai umpan pembaca -- sebelum ini penulis tidak bisa tahu tampilan pengumumannya sebelum menerbitkan. Statistik baca ("Dibaca X dari Y") sudah ada di tabel kelola sebelum sesi ini, tidak diubah. Diverifikasi ujung-ke-ujung dengan API dan web sendiri (port 8103/3103) memakai pengguna sementara `w2c-admin` (dibuat lewat API dari akun admin bersama, diarsipkan lewat `POST /v1/users/{id}/archive` di akhir sesi karena API tidak punya hapus permanen) terhadap Postgres dev bersama; tiga tagihan dan dua pembayaran sementara (mendemonstrasikan keempat status tagihan untuk satu siswa) dan empat kunjungan tamu sementara (mendemonstrasikan papan terisi dan pencarian) dihapus dari database setelah tangkapan layar -- jumlah baris `bills`/`visitor_visits` diverifikasi kembali ke angka semula.
Sesi paralel 24 September 2026 (menutup tiga item "belum dikerjakan" dari sesi kesiswaan di atas, kontrak-dulu API + web, migrasi `0115_violation_attachments`). **Bukti foto pelanggaran**: `violation_attachments` (tenant_id, violation_record_id, asset_id) mengikuti pola persis `counseling_attachments` -- endpoint presigned upload-url/confirm/list/url baru di `openapi/modules/discipline.yaml` (`/v1/discipline/violations/{recordId}/attachments...`), `service.RequestViolationAttachmentUpload`/`ConfirmViolationAttachment` (re-encode JPEG/PNG untuk buang EXIF, batas 10 MB dan 3 foto per catatan, `ErrAttachmentLimitReached` baru) meniru `counseling.go` persis; web: `ViolationAttachments` (kamera `capture="environment"`, thumbnail lewat URL bertanda tangan yang diminta lazy per lampiran) dipasang di dialog detail baris pelanggaran baru di `StudentDisciplineView` (klik baris membuka catatan + lampiran, ganti tampilan datar sebelumnya). **Pratinjau poin langsung**: endpoint baru `POST /v1/discipline/points-preview` (`x-permission: record_violations`, satu query `ListPointsPreviewForStudents` untuk seluruh siswa terpilih sekaligus lewat `= any(...)`) mengembalikan total poin aktif dan level SP yang sudah diterbitkan per siswa plus kebijakan SP tenant -- poin yang akan ditambahkan dan level yang baru tercapai dihitung di klien (`lib/points-preview.ts`'s `computePointsPreview`, murni dan diuji, meniru `SPPolicy.DueLevels` Go persis dengan pola yang sama seperti `computeLiveAverage` gradebook) karena klien sudah punya poin tiap jenis pelanggaran dari katalog yang sudah diambil; dipasang di `ViolationRecordForm` sebagai panel yang diperbarui otomatis saat siswa/jenis dipilih, sebelum simpan. **Pencatatan piket manual (izin keluar/terlambat tanpa QR siswa)**: tidak ada tabel baru -- `RecordExitPermitByStaff`/`RecordLateArrivalByStaff` (`apps/api/internal/modules/permits/service/staffrecord.go`) membuat instance alur kerja yang sama lalu langsung menutupnya dalam satu langkah (`created_by` guru piket bukan siswa, satu event `verification: manual` dengan catatan penjelas), menghasilkan baris `exit_permits`/`late_arrivals` yang tidak bisa dibedakan strukturnya dari hasil alur QR biasa -- otomatis muncul di daftar/laporan yang sama tanpa perubahan lain. Endpoint baru `POST /v1/exit-permits/staff-record` dan `POST /v1/late-arrivals/staff-record` (`x-permission: issue_scan_tokens`, permission meja piket yang sudah ada); tab baru "Catat manual" di `DutyView` (`duty-manual-record-panel.tsx`) dengan pencarian siswa, pilihan izin keluar/terlambat, dan field sesuai jenis. Diuji integrasi Postgres asli untuk ketiganya (`violation_extras_integration_test.go`, `staffrecord_integration_test.go`) plus unit test murni untuk `computePointsPreview`; migrasi 0115 diverifikasi naik/turun/naik. Bukan diverifikasi: pengecekan visual manual di browser pada 390px/1280px dengan `pnpm dev:docker` (tidak dijalankan sesi ini) -- validasi terbatas pada `go test`/`pnpm typecheck`/`pnpm lint`/`pnpm test`.

Posisi terakhir: 25 September 2026 (sesi impor user upsert). Menutup item "belum dikerjakan" terakhir yang tersisa di tabel paritas: `PreviewUserImport`/`CommitUserImport` (`openapi/modules/identity-admin.yaml`) menerima `mode` (`create`, baku, perilaku lama persis) dan `update_roles`. Mode `upsert` mencocokkan setiap baris ke user tenant yang sama lewat username (satu-satunya kunci yang dipakai aplikasi lama juga -- NIS/NIP tidak dipakai untuk mencocokkan); baris yang cocok memperbarui `name`/`email`/`phone` dan kolom profil sesuai jenis (`identity/service/import_merge.go`'s `mergeProfileFields`/`mergeStringField`), dengan aturan sel kosong berarti "biarkan", bukan "kosongkan" -- supaya berkas yang sebagian kolomnya sengaja dikosongkan tidak menimpa data yang sudah ada. Username dan password tidak pernah ditulis ulang oleh baris update; peran juga tidak disentuh kecuali `update_roles=true` dan pemanggil punya `manage_permissions` (dicek sekali di awal lewat `actorHasPermission`, bukan lewat `loadPrincipal` yang penuh supaya tidak bergantung pada `AcademicYearReader`) -- kalau syaratnya tidak terpenuhi, seluruh permintaan ditolak (`ErrImportRoleUpdateForbidden`) sebelum satu baris pun dievaluasi, bukan diam-diam mengabaikan kolom peran. Baris yang cocok tapi tidak ada bedanya dilaporkan `action: "unchanged"` dan tidak menulis apa pun saat commit. Pratinjau dan commit berbagi evaluasi yang sama persis (`evaluateImportRows`) sehingga tidak pernah berbeda hasil. Sekalian memperbaiki dua celah yang ditemukan saat mengerjakan ini: kolom `phone` di templat sudah diparse sejak awal tapi tidak pernah ditulis ke user yang dibuat (`NewUserRecord.Phone` tidak pernah diisi) -- sekarang ditulis untuk create maupun update; dan username yang dibuat otomatis (baris tanpa kolom `username`) tidak pernah muncul di respons commit karena snapshot hasil diambil sebelum baris ditulis, bukan sesudah. Entri audit `user.import_commit` sekarang membawa `mode` dan hitungan `created`/`updated`/`unchanged`, bukan penanda kosong. Web (`apps/web/features/school`): layar impor mendapat pemilih mode dan saklar "Perbarui peran juga", tabel pratinjau menampilkan aksi per baris (Akan dibuat/Akan diperbarui/Tidak berubah/Tidak valid) dan daftar kolom yang berubah untuk baris update, dan ringkasan jumlah create/update/unchanged di atas tabel. Diuji: `go build`/`go vet`/`go test ./...` (Postgres asli lewat testcontainers) lulus semua, termasuk tiga test integrasi baru (`import_upsert_integration_test.go`: create-lalu-update dengan bukti username/password/NIS tidak berubah, konflik email dengan bukti rollback penuh saat satu baris gagal, dan peran ditolak/diizinkan sesuai `manage_permissions`); `pnpm typecheck && pnpm lint && pnpm test` di seluruh monorepo lulus. Tidak ada migrasi baru (tidak ada perubahan skema). Bukan diverifikasi: pengecekan visual manual layar impor di browser sungguhan.

Posisi sebelumnya: 24 September 2026 (sesi UI/UX presensi guru). Merombak alur "guru mengisi presensi" (`apps/web/features/attendance`) untuk target < 30 detik/36 siswa di ponsel: daftar sesi hari ini kini terurut waktu dengan penanda "Sedang berlangsung"/"Berikutnya", progres isi (`roster_count`/`entered_count` baru di `AttendanceSessionSummary`, dihitung sekali per sesi dari `ListActiveEnrollments`/`ListEntriesBySession` yang sudah ada, tanpa query SQL baru), dan status "Belum diisi"/"Tersimpan pukul HH:MM"/"Terkunci"; desktop memakai layout dua kolom (daftar plus ringkasan hari). Roster diri: kontrol status jadi segmented control berwarna (token `packages/ui-tokens` status, fallback warna dari `AttendanceStatusDef.color` untuk kebijakan status kustom tenant), panah dan angka 1-9 memindah fokus/memilih status, ikon pelanggaran (shield) dapat tooltip, avatar inisial dan NIS (field `nis` baru di `AttendanceRosterItem`, diambil dari `student_profiles` yang sudah ada), header menampilkan tanggal Indonesia lengkap dan jam pelajaran, bar ringkasan status jadi chip berwarna yang juga berfungsi filter (sticky saat scroll, menggantikan toggle "hanya tidak hadir"), aksi massal "Tandai semua hadir" (tidak menimpa Sakit/Izin/Dispensasi yang sudah tercatat, hanya Alpha yang disapu balik) dan "Kosongkan perubahan", baris berubah ditandai titik kecil, draf diketik tersimpan ke localStorage per sesi (dipulihkan bila ditemukan) dan dihapus begitu tersimpan di server, jurnal dipisah ke tab sendiri dengan indikator terisi, dan bar simpan bawah dapat notifikasi sukses berwaktu plus tombol "Coba lagi" saat gagal. Ditelusuri di `reference/sion-rebuild-go/frontend/app/attendance/page.tsx`: tidak ada tombol "Centang H Semua" maupun kode status "PMT" (PMT hanya disebut di PRD sebagai modul LMS suspensi siswa yang terpisah, tidak pernah diimplementasikan di kode); tidak ada rekap A/I/S/D per siswa di kode lama (hanya "PS" -- status pertemuan sebelumnya, sekarang diadopsi sebagai catatan kecil di baris bila statusnya bukan hadir); tidak ada ambang batas alpha; tombol pelanggaran memakai warna primer, bukan oranye. Ambang batas alpha dan tombol oranye tidak diimplementasikan karena tidak ada dasar di kode referensi. Rekap per siswa tetap dibangun sebagai fitur baru (bukan porting) karena berguna: `AttendanceRosterItem.year_counts` baru (query agregat tunggal `CountEntryStatusesForStudentsInYear`, satu round-trip untuk seluruh roster, dihitung lintas kelas/mapel dalam tahun ajaran aktif) dirender sebagai chip mini berwarna per status bukan-hadir (`StudentYearRecap`) di samping nama siswa.

Posisi sebelumnya: 24 September 2026 (sesi lanjutan, lima perbaikan kecil lintas modul). Laporan nilai anak di `/children` tadinya menampilkan rata-rata umum tanpa rincian per mata pelajaran karena orang tua tidak punya izin `view_academic_data` untuk membaca `/v1/academic/subjects` sendiri — respons `GET /v1/children/{studentId}/grades` sekarang menyertakan `subject_name` (family module mendapat `SubjectReader` baru, adaptor `wiring.FamilySubjects` memanggil `academic.GetSubject` yang sudah ada), dan baris per mata pelajaran selalu tampil. Pemindaian kamera di `BarcodeScannerField` (meja perpustakaan, `/classroom-entry`) hanya menampilkan tombol kamera bila `BarcodeDetector` ada, yang tidak pernah ada di iOS Safari — tombol kamera sekarang muncul bila `getUserMedia` tersedia, dengan `@zxing/browser` (pembungkus terawat atas decoder multi-format ZXing) dimuat lambat di dalam efek kamera sebagai fallback ketika `BarcodeDetector` tidak ada, memakai `decodeFromStream` di atas `MediaStream` yang sama. `reportdoc.Section` mendapat field `Columns` opsional yang menimpa `Document.Columns` khusus section itu (`RenderPDF` memilih orientasi dokumen dari kolom terlebar di section mana pun, `Apply` melewatkan section berkolom sendiri tanpa disaring `Options.Columns`) — dipakai memindahkan ringkasan akreditasi perpustakaan (`ExportCatalogueSummaryReport`, dua section berkolom beda) ke `reportdoc` penuh (kop laporan, XLSX/PDF, lokalisasi id/en, `ReportExportDialog` dengan prop baru `columnsCustomizable={false}`), dan laporan bulanan perpustakaan (`MonthlyReportPDF`, layout grid-indikator-plus-tiga-tabel yang tidak cocok jadi satu `Document`) mendapat kop laporan/tanda tangan tenant lewat helper baru `reportdoc.DrawLetterhead`/`DrawSignature`, dipanggil langsung di atas `fpdf.Fpdf`-nya sendiri tanpa mengubah layoutnya. Field `education_level` tenant kini ikut di `TenantBranding` (dan karena itu `GET /v1/me`), sehingga field "Jenjang" di `/setup` terisi otomatis alih-alih selalu "Pilih". `/library/copies` mendapat entri sidebar "Eksemplar" (permission `view_library`, sama seperti "Katalog") karena sebelumnya tidak terdaftar di navigasi sama sekali sehingga highlight jatuh ke "Dasbor".

Posisi sebelumnya: 24 September 2026 (sesi pemindahan laporan ke `reportdoc`). Sesi itu memindahkan lima jenis laporan (`discipline.points`, `discipline.warning_letters`, `grading.report_scores`, `permits.leave_requests`, `permits.exit_permits_yearly`) ke `reportdoc` bersama, paralel dengan agen lain yang memindahkan `attendance.daily` sebagai referensi (kontrak dan daftar periksa ada di [dokumen 05](05-shared-components.md) bagian 10). Setiap laporan kini punya `Key` kolom yang stabil, label Indonesia, judul dan baris cakupan (Kelas/Angkatan/Mata Pelajaran/Semester/Tahun Ajaran), serta cakupan `grade_level_id` (satu section per kelas di angkatan itu) selain `class_id` yang sudah ada, saling eksklusif (400 bila keduanya dikirim). `grading.report_scores` di cakupan angkatan memvalidasi mata pelajaran memang diampu di tingkat itu (400 bila tidak) dan menyatukan kolom komponen penilaian lintas kelas. Kategori dan status izin di laporan `permits.*` kini label Indonesia ("Sakit", "Disetujui", dst.), bukan kode mentah. Ditemukan dan diperbaiki sekalian: `GetActiveAcademicYearID`/`ActiveAcademicYearLabel` (dipakai agen lain untuk resolusi kelas per-angkatan) diam-diam gagal dengan RLS bila dipanggil tanpa transaksi tenant yang sudah terbuka — diganti `GetActiveAcademicYear` yang membuka transaksinya sendiri, dibuktikan lewat test integrasi Postgres asli (bukan cuma unit test dengan fake). Jadwal laporan berulang (`report_schedules`) mendapat kolom `format` (xlsx|pdf, migrasi 0112) yang bisa diatur di UI jadwal, dan kini merender lewat jalur `reportdoc` yang sama (format + kop laporan tenant) alih-alih selalu XLSX. Web: `ReportArgsForm` memilih antara kontrol generik (kelas-atau-angkatan) dan formulir ekspor khusus per jenis laporan yang memakai `ReportExportDialog` bersama (format, judul, kop laporan, kolom bisa diatur) untuk empat dari lima laporan; `discipline.points` dan `grading.report_scores` sengaja belum pindah ke dialog karena kolomnya dinamis per tenant (per level SP / per komponen penilaian) dan dialog belum bisa menampilkan daftar kolom itu di muka. Masih terbuka: kop laporan/tanda tangan belum bisa diisi khusus per laporan (mis. Wali Kelas kiri/Kepala Sekolah kanan untuk laporan per-kelas — ini ranah `reportdoc`/pengaturan kop laporan, bukan modul laporan), dan format tanggal/angka di dalam dokumen (Indonesia penuh, "22 September 2026") masih ranah renderer `reportdoc` sendiri.

Posisi sebelumnya: 23 September 2026. Sesi paralel 23 September menyapu UI/UX per grup fitur di ponsel (390 px) dan desktop (1280 px) untuk lima peran, serta menutup endpoint API yang belum punya layar; hasil, daftar layar baru, perbaikan lintas aplikasi, dan hal yang masih terbuka ada di [sapuan UI/UX](analysis/ui-ux-sweep-2026-09-23.md). Sesi 23 September diawali audit statis pra-deploy VPS (keamanan backend, keamanan web dan infra, sidebar dan RBAC per peran, kelengkapan fitur), dilanjutkan checklist keamanan 19 poin dan perbaikannya: API dan worker tersambung sebagai `app_rw` sehingga RLS berlaku, izin platform dicabut dari admin sekolah, cache sesi dibersihkan saat logout, token reset lama dibatalkan, sanitasi HTML klien dan penolakan SVG berbahaya, audit log di BK/keuangan/nilai/dokumen, gosec di CI, dan `compose.vps.yml` masuk repo. Menjalankan API sebagai `app_rw` membuka tiga kelas bug RLS yang selama ini tersembunyi (GUC kosong, fungsi DDL, use case di luar transaksi tenant) yang semuanya sudah diperbaiki; test integrasi kini berjalan sebagai `app_rw`. Temuan, matriks peran x menu, checklist 19 poin, dan langkah deploy ada di [audit pra-deploy](analysis/audit-pra-deploy-2026-09-23.md). Dua item ledger di bawah sudah selesai dan ditandai di sesi itu: ekspor DOCX jurnal kelas dan remote git. Sesi 20 September Di luar paritas, sesi 19 September mengaudit dan memperbaiki performa web; hasil terukur dan statusnya ada di [dokumen 16](16-audit-performa-web.md). Sesi 20 September merapikan layar "Kelas dan siswa": layout setinggi viewport tanpa scroll halaman di desktop (scroll hanya di daftar kelas dan roster), navigasi kelas dikelompokkan per tingkat dalam kartu, header kelas memakai avatar wali kelas plus badge jumlah siswa, tab membawa jumlah, roster satu kolom bergaris dengan avatar inisial dan NIS, dan komponen bersama baru `SearchInput` di `packages/ui`. Perlakuan yang sama lalu diterapkan ke seluruh grup Data Sekolah: `DataTable` bersama mendapat mode `fillHeight` (tabel mengisi tinggi layar, baris scroll internal di bawah header sticky; toolbar-nya kini memakai `SearchInput`), halaman pengguna mendapat avatar di kolom nama, halaman Pembelajaran dan Penugasan beserta sub-tabnya ikut viewport-fit, dan label grup diseragamkan menjadi "Data Sekolah" (sidebar sebelumnya menulis "Master Data" sementara eyebrow halaman menulis "Data Sekolah"). Penyempurnaan lanjutan: tab Tugas Tambahan tadinya menampilkan UUID mentah karena lookup memakai direktori campuran berlimit 500 — kini nama diresolve dari direktori guru dan pegawai, dengan avatar dan hitungan penugasan aktif; baris tab Mengajar memakai badge kelas dan aksi yang muncul saat hover; judul yang menduplikasi label tab di semua sub-view dibuang (rute lama tinggal redirect). Grup Akademik menyusul: kalender akademik jadi viewport-fit (satu bulan selalu utuh tanpa scroll halaman), kenaikan kelas mendapat empty state pengarah dan tabel rencana ber-`fillHeight`, jurnal mengajar ikut viewport-fit, kotak cari gradebook memakai `SearchInput`, eyebrow lima halaman diseragamkan ke "Akademik" (tadinya campuran "Data Sekolah"/"Kelas"/"Presensi"), dan judul halaman plus label sidebar yang Title Case dirapikan ke sentence case (id dan en) sesuai DESIGN.md. Grup Perpustakaan menyusul: katalog dan anggota viewport-fit (aksi katalog jadi ikon, kolom nama anggota ber-avatar), empat tab master data dan jenis anggota mengganti tombol "Ubah"/"Hapus" merah dengan ikon Pencil/Trash2 plus tabel ber-`fillHeight`, laporan peminjaman menerjemahkan status pinjaman yang tadinya tampil mentah ("active"/"returned" kini "Dipinjam"/"Dikembalikan"), dan kata "Edit" yang bocor di copy Indonesia diganti "Ubah" (judul, keanggotaan, diskon). Grup Kesiswaan menyusul: buku poin pelanggaran tadinya menampilkan "Siswa tidak diketahui" untuk sebagian besar baris karena lookup direktori mentok di batas API 500 — batas `/v1/directory/users` dinaikkan ke 5000 (OpenAPI + regen Go dan TS, test Go penuh lulus) dan frontend memakainya, sehingga semua nama siswa terpetakan; halaman pelanggaran, surat peringatan, konseling, dan peringatan dini viewport-fit dengan avatar di kolom siswa; eyebrow halaman izin diseragamkan ke "Kesiswaan" dan judul Title Case dirapikan. Sapuan terakhir menutup sisa grup: Tamu & insiden (tamu dijadwalkan dan log insiden viewport-fit, kartu angka rekap memakai Stat/StatGrid), Kegiatan (ekstrakurikuler, kalender kegiatan, prestasi — plus avatar siswa di prestasi), Keuangan (empat tab pembayaran SPP dengan angka uang tabular), Kelompok mentoring, Siklus supervisi, dan Pengumuman; label grup izin di Peran dan akses yang tampil slug Inggris mentah kini berbahasa Indonesia (12 kunci baru), dan sisa judul Title Case dirapikan (Tamu & insiden, Presensi pegawai, Pusat laporan, OPAC, Konsol platform, Sesi & login). Deskripsi izin masih bahasa Inggris karena datang dari katalog backend; diterjemahkan di tugas terpisah. Sekalian memperbaiki bug lama `Avatar`: palet inisialnya memakai `text-status-*-fg` di atas `bg-status-*` yang warnanya sama persis (token `-fg` adalah warna status sebagai teks di permukaan, bukan teks di atas warna status), sehingga inisial tak terlihat; kini latarnya tint 15% dengan teks warna status.

## Catatan tentang baseline perbandingan

Perbandingan fitur di dokumen ini dan di [Lampiran D](analysis/parity-sion-before.md) memakai kode Go di `reference/sion-rebuild-go` sebagai baseline. Basis data sungguhan yang dipakai sekolah hari ini bukan aplikasi Go itu, melainkan aplikasi Laravel/PHP dari lini yang sama dengan skema yang berbeda di banyak tempat. [Lampiran E](analysis/parity-sion-laravel.md) membaca skema produksi itu langsung, tabel demi tabel, dan menemukan beberapa modul yang aktif dipakai sekolah (koperasi/POS, HBG, diagnostik siswa, izin berkala berkonsen) tanpa padanan sama sekali di newsekolah, serta beberapa modul yang sudah dibangun newsekolah tanpa bukti pemakaian di data produksi. Lampiran D tetap berlaku sebagai catatan asal-usul aturan bisnis; Lampiran E adalah sumber untuk pertanyaan "modul apa yang belum dibangun".

## Ringkasan

Logika backend sudah setara atau lebih baik dari SION di semua modul, dan sudah diverifikasi terhadap database Postgres sungguhan. Setiap operasi API kini punya layar yang memanggilnya, dan tiap layar baru sudah dibuka di aplikasi yang berjalan. Yang tersisa adalah mencoba alur panjang sampai tuntas dengan data dan peran yang tepat.

| Bagian                                  | Status                                                                                                                                                                                                                   |
| --------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| Perbandingan fitur SION dan newsekolah  | Selesai, 72 fitur                                                                                                                                                                                                        |
| Perbaikan logika backend di semua modul | Selesai                                                                                                                                                                                                                  |
| Review independen atas perbaikan        | Selesai, 27 temuan, semua diperbaiki                                                                                                                                                                                     |
| Verifikasi dengan database sungguhan    | Selesai                                                                                                                                                                                                                  |
| Layar presensi disambungkan ke API baru | Selesai                                                                                                                                                                                                                  |
| Layar untuk fitur backend baru lainnya  | Sebagian, 18 operasi belum terjangkau (dari 46 yang diklaim semula; angka benar 20, 2 dikerjakan sesi ini, 9 di luar cakupan, 9 nilainya rendah/berlebih -- lihat [Lampiran F](analysis/openapi-web-gaps-2026-09-25.md)) |
| Uji menyeluruh di browser               | Sebagian, tiap layar baru sudah dibuka                                                                                                                                                                                   |

## Bagaimana pekerjaan ini berjalan

1. **Perbandingan.** Tiap aturan bisnis di handler SION dicari padanannya di kode baru dan diberi status per fitur. Hasil lengkapnya ada di [Lampiran D](analysis/parity-sion-before.md). Dari 72 fitur, saat itu 18 lebih baik, 2 sama, 39 sebagian, dan 13 hilang. Sebelas dari yang hilang ada di perpustakaan.
2. **Perbaikan per modul.** Delapan pengerjaan paralel menutup celah di identitas, akademik, presensi, perizinan, kesiswaan, penilaian, notifikasi, dan perpustakaan.
3. **Review.** Lima review independen memeriksa ulang klaim setiap perbaikan terhadap kode, bukan terhadap pesan commit. Review menemukan 27 masalah baru atau klaim yang tidak sepenuhnya benar.
4. **Verifikasi dengan database.** Seluruh pengerjaan sebelumnya hanya menjalankan test dengan `-short`, sehingga tidak satu pun test integrasi pernah dieksekusi. Menjalankan migrasi, seed, dan test penuh terhadap Postgres menemukan masalah yang tidak tertangkap test unit, termasuk seed yang gagal total dan bug SQL laten pada penyimpanan nilai rapor.
5. **Perbaikan temuan review**, lalu layar presensi disambungkan ke API baru.
6. **Pembangunan layar** untuk kemampuan backend yang belum punya antarmuka, dalam tiga kelompok paralel: perpustakaan, kesiswaan bersama penilaian dan perizinan, serta identitas bersama pengaturan dan dashboard.

Dampak di kode, dihitung dari commit `cb4a908`:

| Ukuran       | Nilai                     |
| ------------ | ------------------------- |
| Commit       | 122                       |
| Migrasi baru | 18, dari 0074 sampai 0106 |
| Operasi API  | 449 menjadi 576           |
| Rute web     | 102 halaman               |

## Yang terverifikasi

- Seluruh 64 migrasi apply bersih di Postgres 17. Delapan belas migrasi baru juga berhasil di-rollback berurutan lalu di-apply ulang.
- `go run ./cmd/seed` selesai di database bersih dan tetap idempoten saat dijalankan dua kali.
- `go test ./...` tanpa `-short` lulus, termasuk test integrasi berbasis testcontainers.
- `pnpm --filter web typecheck` bersih dan `pnpm --filter web lint` tanpa error.
- Layar presensi dilihat langsung dari aplikasi yang berjalan: tampilan guru pada hari sekolah dan tampilan admin yang tidak mengajar.
- `pnpm api:coverage` melaporkan 576 dari 576 operasi punya pemanggil, jadi tidak ada lagi kemampuan API yang tidak bisa dijangkau pengguna.
- Seluruh layar baru dibuka satu per satu di aplikasi yang berjalan pada 16 September 2026. Semuanya tampil tanpa error, dan lima cacat yang ditemukan sudah diperbaiki: halaman impor koleksi gagal 500 karena dependensi belum terpasang di container, satu tombol menampilkan kunci terjemahan mentah, tiga label jenis kunjungan hilang, judul histogram masih menulis UTC, dan komponen kartu membungkus tombol di dalam tombol sehingga memicu galat hydration.
- `cmd/seed` sekarang juga mengisi perpustakaan: jenis bahan, kategori, lokasi, sumber akuisisi, dua jenis anggota (siswa dan guru), siswa dan guru contoh terdaftar sebagai anggota, empat judul dengan 6 eksemplar, satu peminjaman masih berjalan, satu sudah dikembalikan, dan dua kunjungan. Diverifikasi idempoten tiga kali berturut-turut terhadap Postgres sekali pakai (jumlah baris sama persis di setiap run), dan terhadap dev stack: dashboard melaporkan 10 judul, 13 eksemplar, 2 anggota, 2 sedang dipinjam, dan preview impor koleksi dengan kode `BK` menghasilkan status `new_title`. Ditemukan bug laten di `library/service/members.go`: nomor anggota hasil generate bentrok antar jenis anggota karena urutannya dihitung per jenis padahal formatnya (`PS-YYYY-99999`) berlaku satu tenant; seeder menghindarinya dengan nomor anggota eksplisit, perbaikan modulnya sendiri belum dikerjakan.

## Keputusan yang diambil

Keputusan ini sengaja. Jangan diubah balik tanpa membaca alasannya.

| Keputusan                                                                                                                                                                                                                                                                                                                  | Alasan                                                                                                                                           |
| -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------ |
| Guru pemilik jadwal boleh menyimpan presensi setelah jam pelajaran berakhir, sampai tenggat koreksi                                                                                                                                                                                                                        | Aturan SION. Kode baru sempat melarangnya, sehingga guru yang lupa mengisi tidak bisa memperbaiki                                                |
| Presensi bukan data yang dibuat atau dihapus bebas                                                                                                                                                                                                                                                                         | Catatan lahir dari jadwal dan dikoreksi lewat mode koreksi. Tombol hapus akan merusak rekap dan jejak audit                                      |
| Kenaikan nilai rapor dipakai penuh sesuai rentang, dengan batas 0 sampai 10                                                                                                                                                                                                                                                | Aturan SION. Kode baru sempat memotongnya diam-diam ke batas skala                                                                               |
| Guru mana pun yang membuka alur terlambat boleh menyelesaikannya                                                                                                                                                                                                                                                           | Aturan SION. Gerbang review sempat meminta izin yang hanya dimiliki guru piket                                                                   |
| Bukti izin wajib diperiksa saat review dan penerbitan, bukan saat pengajuan                                                                                                                                                                                                                                                | Unggah bukti berjalan dua langkah setelah pengajuan dibuat. Surat tetap tidak bisa terbit tanpa bukti                                            |
| Surat peringatan harus terbit berurutan level                                                                                                                                                                                                                                                                              | Lebih ketat dari SION, yang mengizinkan SP2 terbit sebelum SP1                                                                                   |
| Catatan konseling secara bawaan hanya bisa dibaca penulisnya                                                                                                                                                                                                                                                               | Lebih ketat dari SION demi privasi. Penulis bisa membagikannya ke tim BK                                                                         |
| Jadwal di tahun ajaran terarsip tetap boleh dihapus                                                                                                                                                                                                                                                                        | Data lama yang salah impor harus tetap bisa dibersihkan                                                                                          |
| Hari libur perpustakaan dibaca dari kalender akademik                                                                                                                                                                                                                                                                      | Menghindari dua sumber hari libur yang bisa berbeda                                                                                              |
| Import user: mode `create` (baku) hanya membuat baru; mode `upsert` mencocokkan baris ke user yang sudah ada lewat username saja (bukan NIS/NIP, beda dari rencana awal karena aplikasi lama pun hanya cocok lewat username) dan memperbarui profilnya. Kedua mode tetap maksimal 5000 baris, semua atau tidak sama sekali | Menghindari import setengah jadi; upsert menutup kebutuhan re-import data yang sudah pernah masuk tanpa membuat duplikat                         |
| Import upsert tidak pernah mengubah username atau password user yang cocok, dan tidak menyentuh peran kecuali `update_roles` diminta eksplisit dan pemanggil punya `manage_permissions` -- permintaan ditolak di depan (bukan diam-diam diabaikan) kalau syarat itu tidak terpenuhi                                        | Username dan password adalah kredensial login; peran adalah keputusan otorisasi yang tidak boleh berubah diam-diam lewat berkas impor            |
| Logout menghapus semua perangkat push milik user                                                                                                                                                                                                                                                                           | Skema tidak mengaitkan sesi dengan perangkat. Perilaku ini sama dengan SION                                                                      |
| Berkas XLSX import dibaca di browser memakai `exceljs`, bukan `xlsx`                                                                                                                                                                                                                                                       | Endpoint import menerima baris JSON, jadi parsing harus di klien. `xlsx@0.18.5` punya dua kerentanan tingkat tinggi tanpa versi perbaikan di npm |
| Pembuatan tenant berjalan dalam satu transaksi                                                                                                                                                                                                                                                                             | Tanpa itu, tenant bisa tercipta tanpa tugas wali kelas dan tidak bisa menunjuk wali kelas                                                        |

## Yang tersisa

Diurutkan dari yang paling berdampak bagi pengguna.

### 1. Uji di browser

Setiap layar baru sudah dibuka sekali dan hasilnya tercatat di bagian "Yang terverifikasi". Yang belum dicoba adalah alurnya sampai tuntas, dengan data dan peran yang tepat:

- Perpustakaan: anggota, denda, import koleksi, label batch, kunjungan dan kiosk, lookup ISBN, ekspor.
- Kesiswaan: kandidat surat peringatan, pencatatan pelanggaran banyak sekaligus, lampiran dan topik konseling, laporan PDF siswa.
- Perizinan dan penilaian: antrean izin keluar, pemetaan TP, penyunting rentang nilai, rincian bintang siswa.
- Identitas: dashboard admin, import user, pengaturan sesi, branding dan logo, profil.
- Presensi: popover pelanggaran per sesi, rincian laporan harian, roster wali kelas, detail kalender siswa.

Uji ini butuh akun dengan peran wali kelas, guru BK, pustakawan, dan admin. Data demo perpustakaan (anggota, katalog, peminjaman, kunjungan) kini tersedia lewat `cmd/seed`; modul lain masih menyediakan sebagian, jadi siapkan penugasan tugas tambahan lebih dulu.

Satu hal yang paling perlu dibuktikan di browser: pembacaan berkas XLSX untuk import koleksi dan import user berjalan di sisi klien memakai `exceljs`, dan itu belum pernah dijalankan sungguhan.

### 2. Pekerjaan yang ditunda

| Item                                                                                                                                                                                                                                 | Keadaan sekarang                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| ~~Laporan PDF izin keluar~~                                                                                                                                                                                                          | Selesai. Sudah tersedia sebelumnya lewat pusat laporan generik: `reports/service.RunDocument` merender `permits.exit_permits_yearly` lewat `reportdoc` yang sama dipakai laporan lain, jadi `GET /v1/reports/permits.exit_permits_yearly/export?format=pdf` dan tombol format Excel/PDF di layar Laporan (`ReportExportDialog`) sudah berfungsi -- baris tabel ini sekadar belum diperbarui. Sesi ini menambah bukti ujung-ke-ujung: `TestExportReportExitPermitYearlyPDFHasLetterhead` (Postgres asli lewat `dbtest.Start`/`pg.AppPool`) memanggil `RunDocument` yang sama dipakai `ExportReport`, dengan kop laporan tenant sungguhan tersimpan lewat `UpdateReportHeader`, memverifikasi baris kop di sel A1 XLSX dan keluaran PDF valid (`%PDF`)                                                                                                                                                                                |
| ~~Ekspor DOCX jurnal kelas~~                                                                                                                                                                                                         | Selesai. `scheduling/service/journal_docx.go` merender DOCX asli; dicatat di audit 23 September                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| ~~Kop surat bergambar pada laporan bulanan perpustakaan~~                                                                                                                                                                            | Selesai. `library/service/monthly_report.go`'s `MonthlyReportPDF` kini memuat kop laporan/tanda tangan tenant lewat `reportdoc.DrawLetterhead`/`DrawSignature` (helper baru, dipanggil langsung di atas fpdf.Fpdf-nya sendiri karena layout grid indikator + tiga tabel berbeda kolom itu tidak cocok jadi satu `reportdoc.Document`); tenant tanpa kop laporan/tanda tangan tersimpan tetap dapat fallback lama (nama perpustakaan polos, garis tanda tangan kosong)                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| ~~Barcode gambar pada kartu anggota perpustakaan~~                                                                                                                                                                                   | Selesai. `library/service/print.go`'s `PrintMemberCard` kini merender langsung dengan fpdf (bukan lewat `documents.Renderer`, yang membuang tag `<img>`) memakai layout kartu ATM 2x5 per A4 dari aplikasi lama (`drawMemberCard`), dengan Code 128 gambar nomor anggota lewat `generateCode128PNG` yang sama dipakai label eksemplar (`service/labels.go`)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| ~~Laporan ringkasan akreditasi perpustakaan belum di `reportdoc`~~                                                                                                                                                                   | Selesai. `reportdoc.Section` mendapat field `Columns` opsional yang menimpa `Document.Columns` khusus section itu (dan `RenderPDF` memilih orientasi dokumen dari kolom terlebar di section mana pun); `getLibraryCatalogueSummaryReportXlsx` kini `ExportCatalogueSummaryReport`, dua section (indikator 2 kolom, DDC 3 kolom) dalam satu `Document`, dengan kop laporan, XLSX/PDF, lokalisasi id/en, dan `ReportExportDialog` (mode `columnsCustomizable={false}`, prop baru di dialog itu sendiri)                                                                                                                                                                                                                                                                                                                                                                                                                               |
| ~~Heartbeat presence pengguna online~~                                                                                                                                                                                               | Selesai. `cmd/api/ws.go`'s `wsMeHandler` kini menjalankan `heartbeatPresence` di goroutine terpisah selama koneksi `/ws/me` hidup (setiap 30 detik, `presenceHeartbeatInterval`, jauh di bawah `realtime.Presence`'s ttl 90 detik), bukan cuma sekali saat connect; berhenti bersih lewat `client.Done()` saat koneksi putus (`onClose` yang sudah ada tetap memanggil `presence.Remove`). Kedaluwarsa sisi server sudah ada sebelumnya di `realtime.Presence.Snapshot` (filter ttl in-memory, plus `RedisPresenceStore`'s sorted-set trim per replika) -- sesi ini menambah `presence_test.go` (kedaluwarsa setelah jendela heartbeat terlewat, refresh menunda kedaluwarsa, remove langsung, banyak key independen, mode multi-replika lewat store palsu) dan `ws_test.go` (websocket sungguhan lewat `httptest`, membuktikan `heartbeatPresence` menyegarkan presence selama tersambung dan berhenti bersih saat socket ditutup) |
| ~~Cetak kartu anggota perpustakaan secara massal~~                                                                                                                                                                                   | Selesai. `POST /v1/library/members/cards` (`printLibraryMemberCards`, permission `manage_library_members`) mencetak sampai 500 kartu sekaligus dalam satu PDF A4 (2x5 per halaman), dipilih lewat daftar `member_ids` eksplisit, `member_type_id`, dan/atau `class_id` (keanggotaan aktif); layar Anggota (`members-view.tsx`) mendapat kolom pilihan baris plus bilah cetak (mirip cetak label eksemplar) dan tombol "cetak sesuai filter jenis" saat filter jenis anggota aktif                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| ~~Import user untuk memperbarui user yang sudah ada~~                                                                                                                                                                                | Selesai. `PreviewUserImport`/`CommitUserImport` menerima `mode: "upsert"`: baris dicocokkan lewat username, hanya kolom yang diisi di berkas yang ditimpa (sel kosong berarti "biarkan"), dan pratinjau melaporkan per baris `action` (`create`/`update`/`unchanged`/`error`) plus `changed_fields` untuk baris update. Layar impor di `apps/web/features/school` sudah menambahkan pemilih mode dan saklar `update_roles`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| ~~Validasi skema OpenAPI di middleware~~                                                                                                                                                                                             | Selesai 25 September. `internal/platform/openapivalidate` memvalidasi parameter dan body JSON terhadap spec; `OPENAPI_VALIDATION` bawaan `log` di produksi, `enforce` di dev dan test.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| 9 operasi "get satu"/"tambah-atau-hapus satu" tanpa layar tersendiri (`getClass`, `getAcademicYear`, `getFeeType`, `getWarningLetter`, `getVisit`, `assignStudent`, `createGradeRange`, `deleteGradeRange`, `printLibraryCopyLabel`) | Sengaja tidak dikerjakan -- rincian di [Lampiran F](analysis/openapi-web-gaps-2026-09-25.md). Masing-masing sudah punya jalan setara di UI (endpoint massal yang sudah menangani kasus satu-item, atau field yang datanya identik dengan endpoint daftar yang sudah dimuat), jadi nilainya bagi sekolah mendekati nol                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |

### 3. Risiko terbuka

- ~~**Repo belum punya remote git.**~~ Sudah ada `origin` di GitHub (`omanjaya/newsekolah`).
- **Deploy VPS berikutnya butuh langkah manual.** Kode sudah memakai `app_rw` dan `compose.vps.yml`, tetapi `.env` di VPS harus diisi `APP_DB_PASSWORD` (migrate menolak jalan tanpanya), `DATA_ENCRYPTION_KEY`, dan `TRUSTED_PROXIES=127.0.0.1/32`, lalu `update.sh` dijalankan dengan `COMPOSE_EXTRA_FILES=infra/docker/compose.vps.yml`. Rincian di [audit pra-deploy](analysis/audit-pra-deploy-2026-09-23.md) dan `infra/README.md`.
- ~~**Aksen tenant tidak mengikuti mode gelap.**~~ Sudah diperbaiki. `TenantProvider` kini menulis dua variabel, `--tenant-accent` dan `--tenant-accent-dark`; yang kedua diturunkan oleh `apps/web/lib/tenant/accent.ts` dengan menaikkan lightness sampai menembus 4,5:1 di atas permukaan gelap `#1C1C1C`, sambil menahan saturasi agar tidak menyala. `app/globals.css` memilih salah satunya per tema. Aksen bawaan `#1F3A5F` naik dari 1,48:1 menjadi 4,68:1 (turunannya `#6288BC`). Uji ada di `accent.test.ts`; kaskadenya diverifikasi di browser untuk tiga jalur: sistem terang, sistem gelap, dan `data-theme="dark"` eksplisit.

## Cara memeriksa ulang

Menjalankan migrasi dan seed terhadap Postgres sekali pakai:

```bash
docker run -d --name nsk-check -e POSTGRES_PASSWORD=pw -e POSTGRES_DB=newsekolah -p 55433:5432 postgres:17-alpine
```

```bash
cd apps/api && DATABASE_URL='postgres://postgres:pw@localhost:55433/newsekolah?sslmode=disable' JWT_SIGNING_KEY="$(openssl rand -hex 32)" DOCUMENT_SIGNING_KEY="$(openssl rand -hex 32)" SEED_DEMO=true go run ./cmd/migrate up && go run ./cmd/seed
```

Mencari kemampuan API yang belum punya layar:

```bash
pnpm api:coverage
```

Test penuh, termasuk test integrasi. Butuh Docker aktif, dan jangan pakai `-short`:

```bash
cd apps/api && go test ./...
```

Data demo hanya membuat jadwal Senin sampai Jumat. Layar presensi yang kosong di hari Sabtu atau Minggu adalah perilaku yang benar.
